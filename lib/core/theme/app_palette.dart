import 'package:flutter/material.dart';

class AppPalette {
  static const Color blackColor = Color(0xFF1E1E1E);
  static const Color primaryColor = Color(0xff216fea);
  static const Color whiteColor = Color(0XFFF8F8FF);
  static Color greyColor = Colors.grey.shade800;

  // Background Color
  static const Color backgroundColor = blackColor;

  // Text Color
  static const Color textColor = whiteColor;

  // Button Color
  static const Color buttonColor = primaryColor;
}
